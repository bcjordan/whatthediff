<%= simple_form_for(@page_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, :required => true %>
  </div>

  <div class="form-inputs">
    URLs</br>
    <%= f.text_area :urls, :required => true, :cols => "60", :rows => "25" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
